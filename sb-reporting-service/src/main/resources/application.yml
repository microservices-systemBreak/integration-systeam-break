spring:
  datasource:
    url: jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:6543/postgres?sslmode=require&preparedStatementCacheQueries=0
    username: postgres.nnturimujaxfcclbdfdp
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    queues:
      scan-completed: scan_completed_queue # Nombre de la cola alineado con Core Orchestrator

server:
  port: 8084 # Puerto cambiado a 8084 para evitar conflictos con C#

management:
  endpoints:
    web:
      exposure:
        include: health,info
